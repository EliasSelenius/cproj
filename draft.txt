
todo:
    <done> obj file-hirarchy
    header dependencies
    include folder
    output type - exe, dll, so?
    debug/release build targets
    dependencies (dll, so?)
    colored cli
    embedded resources?
    source generators / prebuild tasks